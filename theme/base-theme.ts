import { createTheme } from "@mui/material";

export const baseTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#002884",
    },
    secondary: {
      main: "#ff7961",
    },
  },
});
